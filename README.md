Static Vercel app.

Use Framework Preset: Other
Build Command: leave blank
Output Directory: leave blank

This version uses a single Domna report PDF as input.
It extracts the first-page summary plus the room-by-room D1 ventilation details from the same PDF.
It generates a Domna-branded `D1 Ventilation` PDF and also lets you download the extracted JSON snapshot.
